# GPU version — install INSTEAD of requirements.txt if you have an NVIDIA GPU
# Step 1: Install PyTorch with CUDA (visit https://pytorch.org for the exact command)
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# Step 2: Install ONNX Runtime with GPU support
#   pip install onnxruntime-gpu
# Step 3: Install everything else
customtkinter>=5.2.2
opencv-python>=4.9.0.80
Pillow>=10.3.0
numpy>=1.26.4
ultralytics>=8.3.0
psutil>=5.9.8
av>=12.0.0
pyinstaller>=6.7.0
GPUtil>=1.4.0
